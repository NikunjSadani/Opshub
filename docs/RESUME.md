# Gifsy OpsHub — Build State (RESUME)
Read this first to continue the build cold. Design: `DESIGN.md` / `design-doc.html`. Plan + governance: `BUILD-PLAN.md`.

> **BIG BUILD IN PROGRESS: the Project Spine** — design `docs/plans/PROJECT-SPINE-DESIGN.md`, tracker `docs/plans/PROJECT-SPINE-BUILD-PLAN.md` (owner decisions locked 2026-08-20). A lightweight ERP (**invoice-first**): Project → client **PO** → priced **line items** → **client invoice(s)** → delivery **challan(s)** → payment, plus vendor credit notes, logistics, Project + consolidated **P&L**, and a **quote/price-book** search over line items. Money spine = ordered→invoiced→paid; delivered is fulfilment-only.
> **✅ Wave 1 DONE (2026-08-20, `471b007`):** new `sales_orders` module + tables (product, purchase_order, po_line_item, po_amendment) + client-master promotion (project_client + client_gstin/address/contact, pan, credit_terms_days) + numbering per-series prefix (migration `6bb6cbb06e4c`, additive). Product Master, Client Master (multi-GSTIN/addr/contact), Purchase Orders (create/bulk-xlsx/amend/short-close/void, **client-supplied po_number**), and Quote/price-book search — BE+FE, RBAC-gated. Adversarial + UI/UX + E2E audited: found+fixed **1 HIGH BE** (cross-client project linkage) + **2 HIGH FE** (add-first-child, stale detail). Gate BE **pytest 463** · FE **vitest 100** · **E2E 11/11**.
> **✅ Wave 2 DONE (2026-08-21, `a695621`; E2E 13/13):** owner reframe — client invoices/credit notes are **created in accounting software and UPLOADED** here (not generated), so billing = **upload → reuse expense pdfplumber Extractor → line-level MATCH to PO lines → review (manual fallback) → confirm** + payments/advances (FIFO-hybrid) + AR aging; `finance` = project + consolidated P&L (net-of-GST revenue − sign-aware cost) + CSV; expense `doc_type` (INVOICE|CREDIT_NOTE) sign-aware for vendor credit notes. New modules `billing`+`finance` (migration `093406be5044`). **DUAL money audit (2 auditors) found+fixed 2 HIGH** (AR counted non-confirmed invoices as receivables; matcher substring-identity false auto-match) + several MED (missing invoiced_qty rollup, delete-with-AR-children 500, TOCTOU locks, retired-line manual match). Gate BE **pytest 524** · FE **vitest 119**. Numbering INV/CN prefix (Wave 1) now DORMANT (invoices uploaded, not minted).
> **✅ Wave 3 DONE (2026-08-21, `0faa96e`):** Fulfilment = **Logistics tracking** only. Owner call: the challan already carries invoice#/PO#/project as text (inc 15), so the link is IN PLACE — the **statutory challan generation is UNTOUCHED**. New `logistics` module + `logistics_shipment` (migr `86fd55b35152`): upload the partner .xlsx dump (or manual) keyed on the **challan number**, UPSERT on re-upload, track status/tracking/partner/**POD**, surface the linked challan's invoice#/PO#/consignee (LEFT JOIN). Gate BE **pytest 539** · FE **vitest 128** · **E2E 15/15**. Audit+E2E caught+fixed a mount-prefix 404, BE↔FE response-shape drift (nested `challan` vs flat, `message` vs `reason`), a bulk-upsert SAVEPOINT gap, + a POD module-key check. ⚠️ **durable: parallel BE/FE agents drift on the response JSON shape unless ONE contract is frozen/shared — the audit + real-stack E2E are what catch it.**
> **✅ Client-CN capture DONE (`1f50e1b`)** — closed the producer-less-consumer gap: a confirmed client credit note can now be captured (upload→extract→match to the referenced invoice's BILLED lines→confirm) and it reduces P&L revenue + AR outstanding + PO invoiced-qty (all three runtime-proven). DUAL money audit fixed a HIGH (CN must credit only billed lines; invoiced_qty floors at 0) + MED/LOW. AR status now reflects CASH (credited-but-unpaid = UNPAID). BE pytest 553 · FE vitest 135 · E2E 17/17.
> **✅ Wave 4 DONE (2026-08-21, `7cd451f`):** the **Action Center** — a read-only `action_center` module computed on read (no tables, no scheduler): procurement follow-ups (T-15 or overdue), invoicing-due (open-to-invoice > 0, reusing the net-of-CN rollup), AR-overdue (reuses `ar_register(overdue)`). Dashboard with count tiles + deep-links. Audit: no HIGH; fixed copy honesty + a defensive CLOSED exclusion. Gate BE **pytest 561** · FE **vitest 140** · **E2E 18/18**. ✅ **PO confirm lifecycle ADDED (`cd1b7a7`):** DRAFT→CONFIRMED (manual, `POST /purchase-orders/{id}/confirm`, OPERATE, + FE Confirm button)→IN_PROGRESS (auto when a client invoice confirms against the PO); Action Center invoicing-due now = status IN (CONFIRMED, IN_PROGRESS). ✅ **short_close residual FIXED (`e322b15`):** `short_closed_qty` now = remaining-open (`ordered − invoiced`, clamped ≥ 0), and Action Center invoicing-due counts OPEN lines only (a retired line never reappears, even after a CN nets its invoiced qty down).
> **🎉 PROJECT SPINE WAVES 1–4 ALL BUILT** (+ both Wave-2 residuals + client-CN capture). ▶ NEXT: **Wave 5 = owner-gated cloud** (live Google-Sheet sync · push email/WhatsApp reminders + scheduler · e-invoice/e-way) — batches with the deploy pass; blocked on GCP/Firebase. Or add a PO confirm lifecycle. Nothing pushed/deployed. **Residuals RESOLVED (`537d7e0`): L1 default race (partial unique index `WHERE is_default` + demote-first) + vendor-CN same-supplier (post-extraction soft review flag).** Client-CN capture BUILT (`1f50e1b`). PO confirm lifecycle BUILT (`cd1b7a7`); short_close remaining-open fix (`e322b15`). **UI/UX audit pass on the two surfaces that shipped without a UI/UX lane (`bc37c4a`, `61c1da5`):** fixed PO-confirm stale-button-on-race + cross-module cache staleness (confirming a CN/invoice didn't refresh AR/PO — present in BOTH billing lanes, fixed at that altitude) + CN copy/error-state nits. **Deferred to owner (surfaced):** (a) pre-confirm billed-vs-credited over-credit tally on the CN review screen (backend already 409s, just late/non-inline); (b) Needs-OCR/Needs-review parity for CNs (banners + field editor like the invoice lane) — both are backend-contract + sizable-UI scope.

## Where we are
**Phase 1 (Delivery Challan) COMPLETE + audit-hardened through inc 25. Phase 2 v1 — Expense/Invoice (inc 24) COMPLETE. Inc 26 = RBAC v2 (custom roles + per-module levels). Inc 27 = expense cost-allocation (required Project + admin-managed Payment method per invoice + a spend dashboard) DONE + audited + E2E.** `develop` @ `4309a48` (code; UNPUSHED, NO git remote — docs commits ride on top, `git log --oneline -1` for the tip). Gate-green (BE ruff 0 · mypy --strict 70 · **pytest 411** · alembic no-drift + migration round-trips · FE tsc + vite build + **vitest 73** · **E2E 9/9 Playwright specs** · `terraform validate` clean). Phase-1 chain: numbering engine → master data → challan generator → faithful L/433 → React frontend → … → contradiction review → resume-aware generate → whole-module audit → User Management (Firebase-pending) → E2E harness → whole-app holistic audit → settings governance → **(24) Expense/Invoice module** → **(25) challan range/list bulk download** → **(26) RBAC v2** (a user holds ONE named ROLE granting per-module levels View<Operate<Manage + platform perms iam/settings; admin Roles editor + Users role-picker; `GET /me` effective-permissions contract drives FE gating, backend enforces). Owner-gated remainder (real Firebase auth · deploy · container WeasyPrint render · GcsStorage · Phase-2 OCR/paid engine) in **Open decisions** + **IMMEDIATE NEXT**. **Newest work first; increments 5–26 are the current build, 1–4 are Phase 0.**

### Open decisions / owner calls
- **`GET /settings` read-gating: RESOLVED by inc 22** (owner asked for the ideal fix). The `Setting` store is now GOVERNED by a code registry (`app/modules/settings/registry.py`): each key declares a visibility (PUBLIC/ADMIN); **writes are admin-only AND restricted to declared keys** (a secret can't be written here — it belongs in Secret Manager); reads are visibility-gated with unknown/legacy keys failing closed to admin-only. `eway_threshold`→PUBLIC. Adversarially audited (no write-bypass/read-leak/enumeration). Commit `5013212`.
- **GCS blob storage for prod (inc-21 security lane, LOW→deploy-blocker):** `get_storage()` always returns `LocalStorage`; `GcsStorage` is an unimplemented stub. On Cloud Run, challan PDFs / ZIPs / source uploads would write to ephemeral disk and 404 after any redeploy/autoscale. **Implement + select `GcsStorage` before the first real deploy** (owner-gated: needs a bucket + creds).
  - **FOLDED IN by owner decision (2026-08-19): make the bulk challan download STREAM/SPOOL to disk (or GCS) instead of building the whole ZIP/2-up-PDF in RAM.** Today [download_challans](../backend/app/modules/challan/routes.py) reads every resolved PDF into memory and `merge_2up`/`zip_files` build another in-memory copy — a maxed 2000-challan `merged` pull is ~300–500 MB transient, and concurrent large pulls could OOM-kill a small Cloud Run instance (an availability blip, NOT lost work — the download is read-only/stateless, a kill = retry; and GENERATION is already incremental-commit + resume-safe so it never loses track of processed vs pending). Owner kept the 2000 cap and chose to fix the memory spike HERE (same read/write path as `GcsStorage`): as each PDF is read, write it into a temp/GCS ZIP or merged file and stream that to the client so peak memory stays flat at ~one PDF regardless of selection size or concurrency. Do this in the same pass as `GcsStorage`.
- **Full mobile nav (inc-21 UI/UX lane, MED, partial):** below the `md` breakpoint the sidebar is hidden; inc 21 made the logo link home as a stopgap, but a proper hamburger/mobile menu is still owed (internal desktop tool, low urgency).

### Open decisions
- **Sparse golden record → blank statutory field: RESOLVED by inc 16** (contradictions are an explicit per-field accept/reject review, not auto-fill).
- **Retry after a partial generation + master-data change: RESOLVED by inc 17.** A retry now IGNORES re-validation errors for already-ISSUED groups (done + immutable) and resumes to COMPLETED; a change to a REMAINING group still blocks (FAILED, issued kept, blocked rows named). A VOIDED group is re-validated (its number is re-minted), never skipped.
- **Register duplicate re-issue: RESOLVED by inc 18 — owner chose WARN.** A group whose (consignee GSTIN, group, date) already has an ISSUED challan surfaces a NON-blocking warning; the operator may still proceed (a genuine same-day repeat is allowed).
- **Accepted staleness (inc-16 audit, both auditors agreed safe, still open):** if an admin edits the golden record *between* review and generate so a field newly diverges, generation prints the (trusted) stored master value for that undecided field without re-review — never an unvetted upload. Bounded + safe; mitigation (bounce back to NEEDS_REVIEW) noted, not built.
- **Accepted residual (inc-18 self-audit, no double-issue, bounded):** recover/sweep act only on a batch idle past the progress heartbeat (recover ≥2 min, sweep ≥60 min), so a live-but-slow render is never touched. A worker that *hangs* (not dead) longer than the threshold, is recovered, then resumes could void the retry's reservations → VOID number GAPS + a spurious FAILED (never a duplicate NUMBER — idempotency + `unique(allocation_id)` + TOCTOU issue all hold). Bounded churn, reversible; not further patched.

### Increment 27 — Expense cost-allocation: required Project + admin-managed Payment method + spend dashboard (orchestrated: schema+migration by me → 2 parallel agents (BE service/routes · FE) → integrate+runtime-smoke → correctness + UI/UX audits + E2E agent → I folded fixes/gated/verified; commits `07e070e`→`cd1cf4c`)
- **What (owner-chosen shape):** every expense invoice is tagged, **at upload for the whole batch**, with a **Project** (from the Projects module, must be Active) + a **Payment method** (an admin-managed list) — **both REQUIRED**; **confirm is blocked** without them. Enables dashboarding by project + payment method. Owner picked project-only (no free cost-category) + a payment-method list; ⚠️ **every expense now needs a project → overhead (rent/utilities) wants a catch-all "General/Overhead" project (owner to create, or ask me to seed one).**
- **Model + migration (`fccb5f50f5a7`, additive):** new `expense_payment_method` (admin-managed, soft-delete via `active`, **case-insensitive-unique** on `lower(name)`) + nullable `Invoice.project_id` (FK project) + `payment_method_id` (FK expense_payment_method). Batch-mode migration, SQLite-safe, round-trips clean. (Nullable so pre-inc-27 rows survive; required enforced at the upload endpoint + confirm guard.)
- **Backend:** payment-method CRUD (`GET` VIEW, `POST`/`PATCH` **Manage**, soft-delete only — no hard-delete so historical invoices keep their method); upload (`POST /expense/invoices`) now takes required `project_id`+`payment_method_id` Form fields, **validated (exist + Active) BEFORE any blob is stored**, stamped on **every** invoice in the batch (rejected rows included); `confirm_invoice` refuses a NULL allocation (409); register + CSV gain Project/Payment columns + `?project_id=&payment_method_id=` filters (CSV-injection guard kept); new **`GET /expense/summary`** aggregates `grand_total_paise` over **CONFIRMED** invoices grouped by project + by payment method (BigInt paise; outer-join NULL bucket so the group totals reconcile to the grand total).
- **FE:** two required pickers on Upload (button disabled + reason until both + a file chosen; honest empty states + a Manage link when no methods exist yet); a Manage-gated **Payment Methods** admin tab; Register Project/Payment columns + filters; an **Overview** dashboard (confirmed-spend tiles + spend-by-project / spend-by-payment tables with bars).
- **Adversarial correctness audit (money path) — NO CRITICAL/HIGH:** allocation is stamped on every persist path (incl. both rejected paths; DUPLICATE persists nothing); confirm is the sole CONFIRMED writer and its guard blocks NULL allocation; summary joins are on PKs (no fan-out → money never multiplies) and reconcile (`sum(by_project)==sum(by_payment)==total`); soft-delete is FK-safe (RESTRICT, no hard-delete); RBAC correct; CSV guarded. **Fixed:** MED (F1) payment-method name uniqueness was app-only + the DB unique was case-SENSITIVE → a `"UPI"`/`"upi"` race could split one cost centre into two dashboard rows → **functional unique index on `lower(name)`**. **UI/UX audit fixed:** MED-1 the Overview "unallocated" bucket rendered as a **blank row** → labeled "Unallocated" (+ honestly-nullable api types); MED-2 the Upload button now says WHY it's disabled (no file / no Operate access); LOW register filters mark inactive methods + non-active projects. **Accepted:** a 413 mid-upload can orphan disk blobs (pre-existing; folds into the GcsStorage cleanup deploy work); confirm doesn't re-check the project is still Active (intended — don't strand a legitimately-uploaded invoice). **Post-inc-27 (`4309a48`): seeded a catch-all "General / Overhead" project (GEN-001, idempotent + a reusable `ensure_overhead_project` helper) and added the project NAME alongside its code on the Overview dashboard + register.**
- **Runtime-verified:** operator 403 on payment-method create; upload 422 without allocation, 400 on unknown/inactive project; summary shape correct. **E2E** `expense-allocation.spec` (create project + method → upload gated until both → register attributes both → confirm → Overview shows the spend) + repaired the pre-existing `expense.spec` (now selects the required pickers; fixed its cross-spec dedup — an `afterEach` cleanup delete was missing its Authorization header and silently 401'd). **Suite 9/9.**
- **⚠️ durables:** (1) a new cross-module FK (`expense_invoice.project_id → project`) needs the target model imported in every test harness that does `create_all`, or `NoReferencedTableError`; (2) a UNIQUE that must be case-insensitive needs a **`lower(col)` functional index** — an app-layer `func.lower` check alone races (this one splits a *money* bucket); (3) an E2E `request.delete` against the dev-auth shim needs BOTH an `Authorization: Bearer` header AND `X-Dev-Uid` — missing the bearer 401s and a `.catch`-swallowed cleanup silently leaves a DUPLICATE that breaks the next same-fixture upload.
- **Gate:** BE ruff 0 · mypy 70 · **pytest 411** · alembic no-drift + round-trips · FE tsc + build + **vitest 73** · **E2E 9/9**.

### Increment 26 — RBAC v2: custom roles + per-module access levels (orchestrated: BE foundation by me → 4 parallel agents (BE gates+tests · FE roles screen · FE users rework · FE gating migration) → integration+runtime-verify → DUAL security audit + UI/UX audit + E2E agent → I folded all fixes/gated/verified; commits `df05894`→`9992ee8`)
- **What (owner-locked design `docs/plans/RBAC-ROLES-DESIGN.md`):** replaced the old fixed `Role` enum + per-user `UserModuleAccess` with a **named ROLE entity** that grants **per-module LEVELS** (`Level` VIEW<OPERATE<MANAGE) + **platform permissions** (`iam` = manage users & roles, `settings`). A user holds **exactly one** role. Owner chose per-module levels **+** a custom role builder, **no per-entity data scoping** (deferred; the `resource` hook in rbac stays reserved).
- **Model + enforcement:** `role` / `role_module_permission` / `role_platform_permission` tables + `user.role_id` (migration `187069733832`). `app/platform/rbac.py` = the single source: `module_level` / `has_at_least` / `require_level` / `require_module` / `has_platform` / `is_administrator` + an **ACTION_CATALOG** mapping each discrete action → (module, min-level) or a platform perm; **default-deny** (unknown action → False). Mutating endpoints now require **OPERATE** (View is read-only); void/delete-confirmed/master-data/seed/client-register = **MANAGE**; users/roles = **iam**; settings = **settings**. Built-in **Administrator** role (`is_system`, all-access, protected — can't edit/delete, last-holder guard) + seeded presets (Challan Operator/Manager, Finance, Viewer) via `roles_builtin.ensure_builtin_roles`.
- **`GET /me`** returns the caller's effective `{role_name, is_administrator, module_levels, platform}` — the contract the FE `usePermissions()` hook gates on (nav + actions) and the future Firebase provider will use. Dev-auth shim gained `X-Dev-Uid` (double-guarded local-only) so every role is exercisable in dev/E2E via the "Act as (dev)" switcher.
- **FE:** admin **Roles** screen (`/admin/roles`, iam-gated) — per-module None/View/Operate/Manage segmented control + platform toggles, clone, guarded delete, Administrator read-only; **Users** reworked to a single Role picker (dropped the enum + module checkboxes); every old `isAdmin` gate migrated to `perms.atLeast(...)`/`hasPlatform(...)`; `RequireRole`→`RequirePlatform`.
- **DUAL security audit + UI/UX audit — all real findings fixed + regression-tested:** (HIGH) a View user could **delete unconfirmed expense invoices** (delete only applied MANAGE when CONFIRMED) → now ≥OPERATE; (HIGH) the migration **collided on Postgres** (`CREATE TABLE role` vs the baseline enum TYPE `role`) → drops the old type first (dialect-guarded; invisible on SQLite tests — the migration blind spot); (HIGH) the migration **back-filled every user to Administrator** → now maps only former ADMINs, others fail-closed role-less; (HIGH UI) a failed `/me` showed a **false "no permission"** → now honest "couldn't load + Retry"; (HIGH UI) **cloning Administrator made an empty role** → seeds full grants; + MED/LOW (deep-link loading race, ungated Retry/Review buttons, editor focus ring, migration downgrade type-orphans). **Runtime-verified:** `/me` per dev user + the backend enforcement matrix (viewer→generate 403, operator→void 403, operator→generate passes gate, viewer/manager→roles 403, admin→roles 200) + live nav re-gating in the browser.
- **⚠️ Accepted / documented (owner-aligned, NOT changed):** `iam` is **de-facto Administrator** (an iam holder can mint+self-assign any non-system role incl. all-MANAGE+settings) — this is exactly the owner's decision that iam bundles full trust; the iam/settings split is convenience, not a security boundary. Preset roles resurrect on rename/delete + re-seed (dev/staging only; prod seed is fail-closed). Case-insensitive role-name uniqueness is app-checked (narrow race, no DB backstop). **⚠️ PROD BOOTSTRAP:** a fresh prod DB has the Administrator role (migration) but NO user holds it until bootstrap creates the first admin — the deploy pass must call `ensure_builtin_roles` + create one admin (seed is fail-closed on prod).
- **⚠️ durables:** (1) a migration touching a native Postgres ENUM is INVISIBLE to a SQLite test suite — a table/type name collision + enum-type orphans only bite on Postgres; verify migrations on Postgres before deploy (owner-gated) or add a PG CI leg; (2) a data-migration backfill must PRESERVE least privilege (map by old role, fail-closed for the rest) — never blanket-assign a privileged default; (3) an auth UI must distinguish "permission denied" from "couldn't load your permissions" — the latter mislabeled is a trust bug.
- **Gate:** BE ruff 0 · mypy 69 · **pytest 403** · alembic no-drift + round-trips · FE tsc + build + **vitest 69** · **E2E 8/8** (added `rbac.spec`; repaired nav-auth/users/projects).

### Increment 25 — Challan range/list bulk download (separate-ZIP or 2-up-merged) (orchestrated: BE by me → FE agent → parallel correctness + UI/UX audits + E2E agent → I folded fixes/gated/verified; commits `8f9b6c8` BE, `e39032c` FE, `f196ba3` audit-fixes)
- **What (owner spec):** a new **Download** tab on the challan module — pick a **series + FY**, enter a from–to **range and/or** a comma-separated **list** of challan numbers (scoped to that series/FY), **Preview** (count + exactly which numbers are skipped), then download **Separate PDFs (ZIP)** of full-A4 challans OR a paper-saving **2-up merged PDF** (2 challans per A4 sheet, each scaled to a half-page — the owner's paper-reduction ask). Backend `app/modules/challan/download.py` (`parse_number_spec` + `resolve`) + `render.merge_2up` (pure-pypdf compositing of the ALREADY-rendered stored PDFs — no re-render, runs anywhere) + `GET /challan/download/preview` + `GET /challan/download?...&mode=separate|merged`. FE `pages/challan/DownloadChallans.tsx`.
- **"Never silently drop a statutory doc" — the invariant the audits hardened.** VOID numbers are skipped and **named** to the operator (`X-Skipped-Void` + amber preview note), never included, never silent. Two independent audits (correctness + UI/UX) both caught the same headline plus more; all fixed + regression-tested:
  - **HIGH:** an ISSUED challan with **no rendered PDF** was counted as downloadable but silently dropped (preview said N, ZIP had N-1). `resolve()` now splits these into a **`no_pdf`** bucket — excluded from the count, reported, never dropped in silence.
  - **HIGH:** one challan whose stored **blob had vanished** (the ephemeral-disk redeploy trap — see the `GcsStorage` open decision) **500'd the entire batch**. The loop now catches a missing StoredFile row / `FileNotFoundError` per challan → **`X-Skipped-Unavailable`**, still delivers every good PDF (404 only if none survive).
  - **MED:** the FE discarded the download's real `X-Skipped-Void`; a challan voided **between preview and download** was skipped invisibly. `downloadUrl` now surfaces both skip headers and the panel **toasts any drift**.
  - **UI/UX:** preview-error retry, honest disabled-reason on error, `aria-live` on the count, required fields + a preview-disabled hint, and the only-voids contradiction ("0 will download" fighting "skipping voided") replaced with one clear line. Param length caps on both routes.
- **Cleared clean by the correctness audit:** cross-series/FY leakage (both in WHERE), void inclusion, header CRLF (`X-Skipped-*` are int-only joins), zip-slip (`_safe()` sanitises the number), auth (`_require_module`), the 2000-cap bypass (union-set re-check). **Accepted residual (MED) — RESOLUTION CHOSEN by owner (2026-08-19):** concurrent large `merged` downloads load up to 2000 PDFs into RAM (bounded by count, not concurrency → a transient Cloud Run OOM risk, but read-only/stateless so a kill = retry, never lost work). Keep the 2000 cap; **fold the streaming/spool-to-disk fix into the `GcsStorage` + deploy pass** (same read/write path) — see the GcsStorage bullet under **Open decisions / owner calls**.
- **Runtime-verified (E2E):** `e2e/challan-download.spec.ts` — self-seeds an isolated series (can't disturb `challan.spec`), generates challans, **voids one**, previews (asserts the skip notice names the voided number), and exercises **both** the ZIP and 2-up-PDF downloads (real `download` events, non-empty files, right extensions). **E2E now 7/7.**
- **Gate:** BE ruff 0 · mypy 63 · **pytest 376** · alembic no-drift · FE tsc + build + **vitest 64** · **E2E 7/7**.

### Increment 24 — PHASE 2 v1: Expense/Invoice module (Document Intelligence, invoice-first, ZERO-cost) (orchestrated: design scouts → Wave-0 foundation by me → 4 parallel build agents → integration + extractor↔gold convergence → DUAL + UI/UX audit → 3 fix agents → money-critical RE-AUDIT → E2E; commits `506f4fe`→`5cfdf7c`)
- **What:** ingest inbound vendor GST tax-invoice PDFs → extract → per-field review → confirm → searchable register. NO numbering (the invoice # is the vendor's). Frozen contract: `docs/plans/EXPENSE-MODULE-DESIGN.md`. Module key `expense_invoice`; `app/modules/expense/` + `frontend/src/pages/expense/`.
- **Document-Intelligence guardrails honoured (DESIGN §4):** engine-neutral **canonical schema** first (`canonical.py`: per-field envelope value/raw/confidence/status/provenance, money=BigInt-paise, arithmetic checks, schema version); **eval harness BEFORE trusting the engine** (`eval/scorers.py` + reportlab synthetic gold + `test_extractor_accuracy.py`); one engine now — `TextLayerExtractor` (pdfplumber, deterministic, **zero-cost**, no OCR/paid API) behind an `Extractor` Protocol (paid/DocAI engine deferred). Deps added: `pdfplumber` (extract) + `reportlab` (TEST-ONLY gold).
- **The eval harness EARNED ITS KEEP:** the extractor passed its own unit tests but the independently-generated gold set caught that it did NOT generalize — first `line_f1 0.0` (borderless tables), then a DUAL-audit round surfaced **5 HIGH** a green suite hid: silent supplier⇄buyer GSTIN swap, borderless right-aligned money misfile, a concurrent-dup upload 500 that dropped the whole batch, a paise/rupee money-input 100× error, and an inert register search. All fixed + regression-guarded by adversarial gold fixtures (each doc_exact 0.0 pre-fix → 1.0 post-fix). A money-critical RE-AUDIT then caught a fix that ITSELF regressed (delete-order FK-violation → 500 on Postgres, hidden by SQLite FKs-off) — fixed + the test harness now enforces FKs.
- **Data model + flow:** `expense_batch/invoice/invoice_line/invoice_field/invoice_correction` (migration `88d04b809c75`). UPLOADED→EXTRACTED→(NEEDS_REVIEW→correct)→CONFIRMED (frozen); NEEDS_OCR parked; REJECTED terminal. **HARD dedup** (unique dedup_key) → per-file DUPLICATE outcome → **delete-and-re-upload** (owner-chosen). Per-file savepoint so one dup never 500s/drops the batch. Corrections (money edited in ₹→paise) flip a field to CORRECTED + feed the gold set. RBAC: module-gated; deleting a CONFIRMED invoice is admin (`expense.delete`); every mutation audited.
- **Runtime-verified (E2E):** `e2e/expense.spec.ts` — upload a real invoice PDF → the real pdfplumber extractor runs → EXTRACTED → confirm → CONFIRMED. 6/6 specs green.
- **Deferred (per design):** standalone `/extract` service · paid/DocAI + OCR engine (scanned invoices park as NEEDS_OCR) · multi-doc-type · buyer-GSTIN validation (needs the owner's entity GSTIN) · multi-invoice single PDF · learned confidence calibration. **Accepted residual:** a concurrent double-submit of a byte-identical un-OCR'd scan lacks a DB unique backstop (best-effort; no money data).
- **Gate:** BE ruff 0 · mypy 62 · **pytest 370** · alembic no-drift · FE tsc + build + **vitest 56** · **E2E 6/6**.

### Increment 23 — E2E contradiction-flow + numbering-sweep-floor (owner-independent follow-ups; commits `2706390`, `78eb131`)
- **Contradiction-review E2E** (`e2e/contradiction.spec.ts`, orchestrated then I verified/finished): the inc-16 headline flow end-to-end + self-contained — generate the template (creates the golden-record parties) → API-edit a stored consignee's name so it diverges → re-upload the SAME template → batch lands in **NEEDS_REVIEW** → resolve every field with "Reject" in the ReviewPanel → save (VALIDATED) → generate → COMPLETED. **E2E now 5/5.** Also fixed a latent `projects.spec` strict-mode flake (assert the register CELL not `getByText` — a success toast also carries the code).
- **Numbering-sweep floor → 6h:** the generic reservation sweep keys off `reserved_at` (not the progress heartbeat), so a `POST /numbering/sweep {older_than_hours:1}` fired mid-render could void a LIVE maximal batch's not-yet-issued tail (≤2000 challans can render >1h) → gap churn (never a duplicate number). Raised the window floor to 6h — safely above any batch render; stuck-batch reservations are already reclaimed far sooner by the batch sweep (60min heartbeat) + `_fail_generation`. Regression test: a <6h window → 422.
- **Gate:** BE ruff 0 · mypy 51 · **pytest 311** · FE tsc + build + vitest 51 · **E2E 5/5**.

### Increment 22 — Settings-store governance (I built + adversarially audited + gated; commit `5013212`)
- Resolves the inc-21 owner-surfaced `GET /settings` finding at the ROOT (owner asked for the ideal, not a light gate). The `Setting` key→value store was ungoverned — any authed user could read every row, and it was the natural place a future secret could land and leak.
- **New `app/modules/settings/registry.py`** declares each known setting + a **visibility** (`PUBLIC` any authed user / `ADMIN` admins-only). `eway_threshold`→PUBLIC (non-sensitive; keeps the owner's "config readable" intent).
- **Guarantees (adversarially audited — verified at runtime, no bypass):** writes admin-only AND restricted to declared keys (unknown key→422, no persist → **a secret has no path into this store**; secrets stay in Secret Manager); reads visibility-gated with a **uniform 404** for hidden keys (existence not leaked/enumerable); unknown/legacy rows **fail closed** to admin-only; `_is_admin` matches `rbac.can()` on the `active` flag (inactive≠admin). Server-side reads (challan engine/seed) use `db.get(Setting,…)` directly — unaffected. **No migration.**
- **One deliberate contract change:** the store is no longer free-form — writes must be to a declared key (adds no real friction; anything the app reads is declared in code anyway). Settings tests rewritten to the governed model + governance tests (10).
- **Gate:** BE ruff 0 · mypy 51 · **pytest 310** · alembic no-drift.

### Increment 21 — Whole-app holistic audit + fixes + CI e2e (orchestrated: 3 app-wide audit lanes + 2 fix agents + my Modal fix + CI wiring → I integrated/gated/committed `150a3ff`)
- **3 independent HOLISTIC lanes** (app-wide, beyond the per-increment passes): security/authz across EVERY endpoint, UI/UX + a11y across EVERY screen, correctness/data-integrity/perf across the backend. **Security lane: NO HIGH — "unusually well-hardened"** (full endpoint×guard matrix clean; dev/stub/e2e shims fail-closed in non-local; sweep secrets constant-time; CSV/XLSX injection guarded everywhere; audit-in-txn complete; zero SAVEPOINT leaks). **Correctness lane: NO statutory/money defect** (numbering core, snapshot immutability, totals all sound under generate/resume/recover/sweep/void/concurrent).
- **HIGH (UI/UX, product-wide) — modal focus theft:** the shared `Modal`'s focus effect depended on an unstable `onClose`, so every parent re-render (i.e. every keystroke in a controlled field) re-ran it and yanked focus back to the FIRST field — typing in any later field was broken across every modal form. Fixed: pin `onClose`/`busy` in refs, depend only on `[open]`. Keystroke-level regression test (`ui/Modal.test.tsx`) that fails on the old code.
- **PERF/correctness:** register **full-scanned on every upload** (dup-warning query) — added `challan` indexes on `challan_date` + `(consignee_gstin, challan_date)` (migration `1e2c7d8a41d7`; also speeds the date-range filter + `/summary`). Bound per-group `total_paise` at VALIDATION (int8 ceiling) so an absurd sum can't burn a number at generate. Capped the 4 master-data list endpoints; escaped LIKE `%`/`_` wildcards in the projects/consignee search.
- **UI/UX:** `/modules` failure now offers retry (Dashboard + Sidebar, was a dead-end); a shared `useDebouncedValue` hook applied to all filter/search inputs (only Register debounced before); a11y (`th scope`, `aria-pressed` on the user active-toggle, `Loading` `aria-live`); download buttons guard double-clicks; the app logo links home (the only route back below `md`).
- **CI:** the reusable `_test.yml` gains an **`e2e` job** (Playwright chromium + `npm run e2e`); `serve-backend.mjs` made cross-platform (`E2E_PYTHON` on the Linux runner). *(The CI job proves out only when the pipeline runs — no remote yet.)*
- **E2E expanded** (`d676f26`, this increment's lead-in): projects create + register status-filter/CSV export; 4 specs total. (Master-data / contradiction / recover E2E descoped — covered by pytest / not UI-drivable.)
- **Owner-surfaced, NOT changed:** settings read-gating (owner chose keep-readable) · `GcsStorage` for prod (deploy prereq) · full mobile nav. See **Open decisions / owner calls**.
- **Gate:** BE ruff 0 · mypy 50 · **pytest 307** · alembic no-drift + migration round-trips · FE tsc + build + **vitest 51** · **E2E 4/4**. Commit `150a3ff`.

### Increment 20 — Local Playwright E2E harness (over the REAL backend via the dev-auth shim) (I built + ran green)
- **`npm run e2e`** (`frontend/playwright.config.ts`) boots TWO webServers and tears them down automatically: the real FastAPI backend against a FRESH sqlite DB (`e2e/serve-backend.mjs`: migrate → `app.seed` → `app.e2e_bootstrap` → uvicorn, with `DEV_AUTH=true` + `STUB_RENDER=true`) and the Vite dev server; `e2e/global-setup.ts` fetches the upload template as a fixture. Specs drive the real SPA↔backend↔DB:
  - **`challan.spec`** — upload the template → validate → **generate → register → void** (the full statutory lifecycle: real reserve+issue+register+void).
  - **`users.spec`** — list seeded users → invite (a "setup pending" row appears) → the last-admin/self-lockout guard returns 409 and the list is untouched.
  - **`nav-auth.spec`** — the dev role switch to a non-admin hides the Users nav AND flips the admin screen to the "Not authorized" panel (RequireRole), all client-side.
- **Enabler — a LOCAL-ONLY gated stub PDF renderer** (`render.get_renderer`): WeasyPrint is container-only, so the E2E (and local dev) use a native-free stub that emits a valid minimal PDF, so the WHOLE generate flow runs without the native lib. **Double-guarded (`env=='local'` AND `stub_render`), fail-closed — staging/prod ALWAYS use WeasyPrint** (`test_render_selector` proves a non-local env never stubs, even with the flag on). `app/e2e_bootstrap.py` seeds the consignor/HSN/projects the template examples reference (local-only, idempotent, fail-closed). vitest excludes `e2e/**` so the two runners don't collide.
- **Gate:** BE ruff 0 · mypy 50 · pytest 302 · alembic no-drift · FE tsc + build + vitest 46 · **E2E 3/3 pass**. Commit `492af50`. (This is the last of the three post-audit items — #1 whole-module audit, #2 User Management, #3 E2E harness — all done.)

### Increment 19 — User Management module (Phase-0 platform capability; Firebase-PENDING) (orchestrated: 2 build agents + dual adversarial + UI/UX audit → I integrated/gated/runtime-verified/fixed)
- **Admin-only user CRUD + per-user module grants**, built on the EXISTING `User`/`Role`/`UserModuleAccess` model — **no migration**. Backend `app/modules/users/` (provisioner · service · routes, mounted at `/api/v1` as a platform primitive, no nav ModuleSpec): `GET/POST/PATCH /users` + `POST /users/{id}/setup-link`, every endpoint admin-gated (`user.manage`). **No password is ever accepted or stored** — accounts are provisioned password-less via a `UserProvisioner` seam (real `FirebaseProvisioner` = `fb_auth.create_user(email)`; `LocalProvisioner` stub for local/dev), and the user sets their own password via a Firebase setup link. **Lockout guards:** the last active admin can't be disabled/demoted; no self-disable/self-demote. Every mutation audited (incl. setup-link issuance — the link URL is never written to the trail). FE `/admin/users` (RequireRole ADMIN + `isAdmin`-gated nav): invite/edit modals, one-time setup-link panel with honest copy (never claims an email was sent), **no password field**.
- **Dual adversarial + UI/UX audit (auth path) — all findings fixed + regression-tested:**
  - **CRITICAL** — `create_user` provisioned the Firebase account BEFORE the DB commit with no compensation → a failure orphaned the account and bricked the email forever. Now a **SAGA**: the service owns the commit + a compensating `delete_auth_user` on ANY failure; the setup link is best-effort post-commit. ⚠️ durable trap: a released `begin_nested` SAVEPOINT is NOT undone by a later rollback on pysqlite — replaced with a plain flush + full rollback (verified with a probe).
  - **HIGH** — last-active-admin guard was TOCTOU (two concurrent demotions → org lockout). The admin-set read is now `SELECT … FOR UPDATE` (ordered), serializing the race (Postgres; sqlite single-writer masks it).
  - **MED** — provisioner selection now NEVER silently uses the local stub in a non-local env (a misconfig fails loud instead of minting un-loginable users); modal **secret-bleed** fixed by conditionally MOUNTING the invite/edit modals (fresh state per open, so user A's setup link can't flash in user B's dialog).
  - **LOW** — `coming_soon` modules no longer grantable (FE+BE filters aligned); re-issue toast honest when no link; error toasts `role="alert"`; honest null-link copy.
- **Runtime-verified through the real SPA** (dev-auth shim, LocalProvisioner): screen renders with admin-gated nav; invite → real HTTP create → `local:` user + honest null-link panel + list refresh + "setup pending"; self-disable of the last admin → **409 at the backend, list untouched, modal stays open**, clean console. **▶ Firebase-PENDING:** the real invite→set-password→login can't be end-to-end verified until the owner wires Firebase (replace the mock AuthProvider + supply `firebase_credentials_file`/`firebase_project_id`); stub-created `local:` users would then need re-provisioning (documented limitation, `is_provisioned=false` surfaced as "setup pending").
- **Gate:** BE ruff 0 · mypy 49 · pytest 300 · alembic no-drift · FE tsc + build + vitest 46. Commit `eb05612`.

### Increment 18 — Whole-module adversarial audit + hardening (orchestrated: 5 parallel audit lanes + 3 fix agents + my own statutory-path fixes + a follow-up self-audit → I integrated/gated/runtime-verified)
- **5 independent audit lanes** across the inc 14–17 seams (numbering+resume · consignee+contradiction · money+parsing+injection · FE/UX · cross-cutting authz/audit). **Statutory-integrity core verified sound in every lane** (no duplicate NUMBER possible under any interleaving; audit chain intact; zero SAVEPOINT leaks; idempotency + `unique(allocation_id)` + TOCTOU issue all hold). Then a 6th lane adversarially audited the fixes themselves.
- **HIGH — wedged-generation recovery (the real dup-number exposure):** a crash/OOM/deploy that drops the in-process worker left a batch stuck in `GENERATING` with no exit but re-upload → fresh numbers for already-issued groups → duplicate challans. Fix: `recover_stuck_batch` + admin `POST /challan/batches/{id}/recover` (`challan.recover`), unattended `sweep_stuck_batches` + secret-gated `POST /challan/batches/sweep-stuck`, a **per-challan progress HEARTBEAT** on `batch.updated_at`, and **staleness guards** so neither recover (idle ≥`_STUCK_AFTER`=2 min) nor sweep (≥60 min) can act on a live-but-slow render. FE: **Retry** (FAILED) + **Recover** (stuck GENERATING, admin) from the Batches tab. **No migration** (reuses existing statuses/columns).
- **HIGH — consignee same-GSTIN consistency:** the check compared every row to `grp[0]`, so if the first row left an optional field blank, two later rows with DIFFERENT values passed and one was silently dropped — printing a never-entered address. Fix: track **first-non-empty per field** across the whole group (mirrors `_merged_consignee`).
- **Owner-chosen register duplicate WARN** (non-blocking) on a re-issue of an already-ISSUED (consignee GSTIN, group, date); `validate(exclude_batch_id=...)` excludes the batch being validated (never self-warns on resume).
- **MED:** PII/SQL leak (`str(err)` → `batch.message`) — now generic message + full detail to logs only + `hide_parameters=True` + scrubbed numbering-contention message/audit detail. European decimal-comma + non-ASCII digits rejected; quantity column-bounded. Stale review `.xlsx` no longer shown as "warnings" after a resolved review. Non-admin `UPDATE_MASTER` radio disabled in the review panel.
- **LOW:** `/files/upload` authz gate; `submit_decisions` admin gate at the service boundary; `with_for_update` on golden-record updates; `/projects` idempotency; Batches polls while GENERATING; group-key normalization; register debounce/load-more/CSV-truncation surfaced.
- **Self-audit of the fixes found 2 MED (both fixed at the root):** sweep used `updated_at` as a liveness signal but the render loop never touched the batch row → a healthy large render could be swept mid-flight (→ tempting a duplicate-minting re-upload); fixed by the progress heartbeat + staleness guards + a 60-min floor. Regression-tested at service + HTTP level (recover/sweep/consistency/dup-warn/authz).
- **Gate:** BE ruff 0 · mypy 45 · pytest **281** · alembic no-drift · FE tsc + build + vitest **40**. Commit `4c5d200`.

### Increment 17 — resume-aware generate (reconcile retry-after-partial-generation) (backend-only; I built + gated + runtime-verified + adversarially audited)
- **On a retry of a partially-generated batch, re-validation IGNORES errors for groups whose challans are already ISSUED** (done + immutable) — so a master-data change to a *completed* portion no longer wedges finishing the rest; the batch resumes to COMPLETED. A change to a still-REMAINING group is kept and blocks (FAILED, issued challans preserved, blocked rows named). The reserve/issue machinery is unchanged + already resume-safe: `numbering.allocate` is idempotent on an already-issued group's key (returns the existing allocation → `_existing_challan` reuses the challan → **no statutory number is ever re-minted for an issued group**).
- `Challan.group_key` column (migration `6598f029a98e`, `server_default ''` — safe over existing rows; pre-inc-17 rows opt out). `validate(skip_error_groups=...)` + `_filter_resume_errors` + `_issued_group_keys` (**ISSUED only**).
- **Adversarial audit — idempotency confirmed solid (no double-issue). Fixed the HIGH it found:** `_issued_group_keys` must EXCLUDE VOID challans — a voided group's number is re-minted on retry, so it MUST be re-validated, never skipped as "done" (else a voided challan could be re-issued bypassing HSN/project validation). Same status filter on the FAILED-vs-FAILED_VALIDATION count. Regression-tested (void-then-retry re-validates; master-change-to-issued-group resumes; master-change-to-remaining blocks).
- **Gate:** BE ruff 0 · mypy 45 · pytest ~245 · alembic no-drift. Runtime-verified through the real service. Commit `f53809d`.

### Increment 16 — consignee contradiction review (accept/reject per field) + Excel report (orchestrated: backend by me + FE agent + DUAL adversarial audit + UI/UX audit → I integrated/gated/fixed/runtime-verified)
- **New `NEEDS_REVIEW` batch state (blocked from generation)** + `DecisionChoice` (UPDATE_MASTER / THIS_UPLOAD / REJECT, default PENDING) + `challan_batch_decision` table (one row per batch/gstin/field). Migration `8a1ab1024872`. When an uploaded consignee's details contradict the **stored golden record** (known GSTIN), each differing field is a decision the operator must resolve; a batch flips to VALIDATED only when **none remain PENDING**. **SAME GSTIN with inconsistent details in one upload = a blocking error** (guarantees each field has one "your value"). Row cap raised to 50,000 so the **2,000-challan cap is the only real limit**.
- **The three outcomes** (runtime-proven): `UPDATE_MASTER` permanently updates the shared golden record + prints the uploaded value; `THIS_UPLOAD` prints the uploaded value on this batch only (master untouched); `REJECT` keeps + prints the stored value. **A PENDING/undecided field can never print an unvetted upload** — it defaults to stored. The **golden-record write is deferred to batch SUCCESS**, so a failed batch never mutates the shared master. A new GSTIN auto-creates from the **merged superset** across groups (no blank drops another group's value). Downloadable **Excel** review report (uploaded rows + a contradictions sheet), formula/DDE-injection-guarded.
- **FE:** `ReviewPanel` (own file, reusable) — per-field 3-way choice grouped by consignee, "N of M decided", Excel download, blocked until all decided; reachable from **both** the upload flow **and** the Batches tab (never a stuck dead-end). "Update master" carries an explicit permanent/shared-consequence hint + a confirm dialog; options ordered least-destructive first.
- **DUAL adversarial audit + UI/UX audit — no HIGH in the core gating (confirmed: a PENDING field always defaults to stored, never an unvetted upload). Fixed + regression-tested:** HIGH Excel formula/DDE-injection guard; HIGH UI/UX (a NEEDS_REVIEW batch was resolvable only in the upload session → now from the Batches tab; "Update master" consequence made honest); MED UPDATE_MASTER no longer mutates the master before generation succeeds; MED control-char cell no longer 500s the upload (stripped at coercion); MED new-GSTIN merged superset; MED UPDATE_MASTER **admin-gated** (shared-master mutation, matching the direct master-edit route); duplicate decision ids → 422; submit row-locked; + UI/UX MED/LOW (empty-decisions escape, 409 refetch+inline error, progress counter, a11y fieldset labelling, friendly status labels).
- **Gate:** BE ruff 0 · mypy 45 · pytest ~240 · alembic no-drift · FE tsc + build + vitest 37. **Runtime-verified end-to-end** (fake renderer): known-GSTIN diff → NEEDS_REVIEW + 2-sheet Excel; generation blocked until decided; all three outcomes print exactly right; same-GSTIN inconsistency → blocking error. Commit `4669341`.

### Increment 15 — 26-col template · inline GSTIN consignee · Project ID · warnings (orchestrated: contract by me + parser/render/template/FE agents + DUAL adversarial audit + UI/UX audit → I integrated/gated/fixed/runtime-verified)
- **26-column upload template** with friendly headers (`schema.COLUMN_HEADERS` + an alias map so hand-adjusted spellings still parse), explicit **`Challan Group`** grouping column, structured ship-to (line1/line2/city/pincode/phone — stored split, **printed joined**), inline consignee (name/addr/pincode/state/phone/**GSTIN**), **Project ID**. **`brand` DROPPED** (the inline GSTIN identifies the bill-to party). The two `Pin code` headers are now distinct (`Ship-to Pincode`/`Consignee Pincode`), `State`→`Consignee State`, `ship_to_contact` dropped.
- **Inline consignee via the golden record** (`consignee_master.resolve_or_create`): an unknown GSTIN **auto-creates at GENERATION** (a new `dry_run=True` mode means validation of a batch that later fails leaves NO golden records); a known GSTIN with different typed details is a **non-blocking DEVIATION WARNING** and the **STORED record is snapshotted** (never the typed value). **Project ID validated** (`projects.resolve_active_project` — must pre-exist + be **Active**, else a blocking error) and **printed** on the challan. **Possible-split WARNING** when two groups ship to the same consignee+destination+date (date-format aware). **Intra-batch new-GSTIN conflict WARNING** (same new GSTIN, two groups, differing details). Legacy brand→state resolution retired from the parser; the legacy `Consignee` table remains (untouched).
- **Migration `3dab8e347c22`** — `challan` gains `project_code` + structured ship-to columns; `consignee_brand`→nullable (retained for pre-inc-15 issued rows, no longer written). Safe over existing issued rows (`server_default`), SQLite batch-mode + Postgres direct ALTER, downgrade round-trips.
- **DUAL adversarial audit + UI/UX audit — core CLEAN** (dry_run never touches the session; no party created during a failing validation; project Active re-checked at generate; money invariants intact; migration safe). **Fixed: HIGH** `parse_paise` 500 on a huge finite cell (e.g. `1E30`) + money sanity ceiling; **HIGH** golden-record UPDATE path now enforces GSTIN↔state (422); **MED** intra-batch new-GSTIN conflict now warns; **MED** retry-after-partial preserves issued challans; **LOW** collision date-format aware + pincode rejects non-ASCII digits; **UI/UX HIGH** warnings-report label is status-derived (not "Error report" on a succeeded batch). Every fix is regression-tested.
- **Gate:** BE ruff 0 · mypy 44 · pytest ~225 · alembic no-drift · FE tsc + build + vitest 34. **Runtime-verified end-to-end** (throwaway harness, fake renderer — WeasyPrint is container-only): the app's own generated template → validate → generate 2 challans; Project ID + joined address printed in the HTML, no brand; both consignee golden records auto-created at generate; re-upload same GSTIN different name → deviation warning + stored record snapshotted; two groups same destination → split warning; unknown project + invalid GSTIN → blocking errors. Commit `28b627d`.

### Increment 14 — Consignee golden-record master (GSTIN-keyed) (orchestrated: backend + frontend agents + 1 identity-path audit → I integrated/gated/fixed/runtime-verified)
- **NEW GSTIN-keyed consignee master** (`ConsigneeParty` in `app/modules/masterdata/`, table `md_consignee_party`, migration `d61ab46c13de`) — **additive; the legacy brand→state `Consignee` and the challan parser are untouched** (inc 15 switches the parser + retires the legacy resolution). Core engine `consignee_master.resolve_or_create`: an **unknown GSTIN auto-creates (source UPLOAD, NO admin verify)**; a **known GSTIN with different details returns the STORED record + a deviation report and NEVER overwrites** (text via `match_key`, pincode/phone digits-only, empty-incoming ≠ deviation). Reuses hardened `valid_gstin` checksum + `collapse_ws`/`match_key`. Admin CRUD (`create_party`/`update_party`, GSTIN immutable, `apply_incoming` to accept a deviation), routes (`GET/POST/PATCH /masterdata/consignee-parties`, admin writes / module-gated reads). FE: a new **"Consignees (GSTIN)"** Master Data tab (search, create/edit, source badge).
- **Independent identity/statutory audit — core CLEAN** (no silent overwrite, deviation sound, gstin normalized-before-uniqueness so no dup dodges the key, race SAVEPOINT-safe get-or-create, authz complete, gstin immutable on PATCH). **Fixed MED-1:** enforce **gstin↔state** consistency on CREATE (a `27…` Maharashtra GSTIN can't store `state=Gujarat`) — mirrors the consignor/consignee validators, matters because inc 15 snapshots this state onto the statutory challan; **LOW-2:** a no-op PATCH no longer bumps `updated_at`/reorders/audits. (Left+noted: LOW-1 intended admin blanking with `name` protected · LOW-3 shared `valid_gstin` positional lenience · LOW-4 `q` LIKE wildcards, parameterized.)
- **Gate:** BE ruff 0 · mypy 44 · pytest 205 · alembic no-drift · FE tsc + build + vitest 32. **Runtime-verified:** auto-create → identical re-submit no deviation → changed name/state deviation (stored unchanged) → invalid checksum rejected → **gstin↔state mismatch 422** → FE tab shows MANUAL + UPLOAD rows, clean console. Commit `7094f15`.
- **✅ inc 15 (the last of the 3-part thread) is DONE** — see the Increment 15 block above. The parser now switches to the GSTIN golden record + structured template; the legacy brand→state resolution is retired (the legacy `Consignee` table itself remains, unused by the parser).

### Increment 13 — Projects module (orchestrated: backend + frontend agents + 1 audit → I integrated/gated/fixed/runtime-verified)
- **New top-level "Projects" dashboard module** (`app/modules/projects/` + `frontend/src/pages/projects/`). An admin registers a **Client** with a unique 3-letter code; users create **Projects** under a client, each getting a **system-assigned ID `<CLIENT_CODE>-<running number per client>`** (`BRI-001`, `BRI-002`, `TAT-001`). Per-client row-locked sequence reusing the numbering engine's *discipline* (NOT the statutory challan counter) with `UNIQUE(code)` + `UNIQUE(client_id,seq)` backstops. Routes: admin client-create + status PATCH, module-gated reads + project-create. Migration `690e7736007c`; `ModuleSpec(key="projects")`; `project.manage` in `ADMIN_ONLY`. FE: Projects list + New-project form + admin Clients screen + `/m/projects` route + a `patch` helper on `useApi()`.
- **Independent numbering/authz audit — ID-integrity core CLEAN** (no dup/gap/cross-client mint possible: two UNIQUE backstops + lock-before-read + normalize-before-uniqueness + SAVEPOINT-safe + all gates present). **Fixed MED:** contention now → 409 (was 422) via `ProjectIdContention`; generic client message + server-side log (no raw DB/constraint leak); documented the READ-COMMITTED dependency + bumped retries 2→5; service-level name-length cap (defense in depth). LOW-3 noted: sqlite/StaticPool tests can't exercise a real concurrent race (the UNIQUE backstops make a dup impossible regardless).
- **Gate:** BE ruff 0 · mypy 43 · pytest 186 · alembic no-drift · FE tsc + build + vitest 32. **Runtime-verified:** `bri`→`BRI`, `BRI-001/002`, `TAT-001`, dup client → 409, `client_id` as a JSON string coerced fine, FE renders all three with clean console. Commit `76065f8`.
- **▶ This is step 1 of a 3-part thread** (from the owner's updated 26-col template): **inc 14** = consignee golden-record master (**auto-create on first sight, NO admin verify**, GSTIN-keyed, deviation-flagging); **inc 15** = new 26-col template + parser (structured ship-to address stored-split/printed-joined, inline consignee w/ deviation detection, **Project ID validated against this module — must pre-exist + Active**, printed on the challan). Header fixes owed in the new template: two `Pin code` cols must be distinct; `State`→`Consignee State`; `ship_to_contact` dropped (confirm).

### Increment 12 — New Challan 3-step redesign + UI/UX audit (orchestrated: 1 build agent + 1 UI/UX audit → I integrated/gated/fixed/runtime-verified)
- **New Challan → numbered 1-2-3 step cards** (`NewChallan.tsx` + a `StepCard` helper): 1 Get template · 2 Upload & validate (VALIDATED summary / FAILED_VALIDATION with the English error report as a **first-class** action) · 3 Number & generate — **disabled-with-reason until a batch validates**. Plus a **Recent batches** list (last 5, artifact links, "View all"). Generate→poll→ZIP/merged and the 60s stall hint preserved. **Design ideas borrowed** from an external "Challan Desk" concept — ONLY the numbered-steps layout, clear disabled states, and recent-batches; we deliberately KEEP our server-reserved numbering (not the concept's typed "starting number") and authoritative validation.
- **Independent UI/UX audit (1 HIGH/2 MED/4 LOW) — money/copy/consistency core clean; fixed:** **HIGH** the `Button` primitive's `disabled ?? loading` ignored `loading` when an explicit `disabled` was passed → double-submit; now `(disabled ?? false) || loading` (**app-wide fix**, `ui/primitives.tsx`) · MED invalidate `['challan','batches']` when the polled batch hits terminal (recent row no longer stuck GENERATING vs Step 3 COMPLETED) · MED a stable `sr-only` `aria-live` region announces each phase · LOW series-required hint, Step 2/3 no-dead-end fallbacks, hoisted shared `batchArtifacts()` (`challanFormat.ts`, also used by `Batches.tsx`).
- **Gate:** FE tsc + build + vitest 28 (BE untouched: pytest 172). Runtime-verified in a fresh tab (3 steps + recent batches render, **clean console**). Commits `291855e` + regression test `deb9c4f` (`ui/primitives.test.tsx` — locks "a loading Button stays disabled even with `disabled={false}` and doesn't fire onClick").

### Increment 11 — Self-documenting upload template (solo)
- **`GET /challan/template.xlsx`** (module-gated; `challan/template.py`) replaces the old header-only client-side CSV — which also mismatched the `.xlsx`-only uploader. Two sheets: **Challans** (data sheet = `worksheets[0]`; styled+frozen header with a hover-comment per column + 2 worked example rows — one single-line priced, one multi-line value-free) and **Instructions** (per-column Required?/Applies-to/what-to-enter **derived from `schema.CHALLAN_COLUMNS`/`REQUIRED_COLUMNS`/`GROUP_CONSISTENT_FIELDS` so it can't drift**, plus Dos & Don'ts). FE **Download template** button uses `downloadUrl`.
- **Round-trip test** (`tests/test_challan_template.py`): the template's OWN example rows parse (`parse_workbook`) + validate (`service.validate`) against seeded master data — C1 tax-inclusive = 105000 paise, C2 value-free (None) — so the examples can never silently rot. + structure + route + module-gate tests.
- Note: the **downloadable English validation error report already existed** (`_error_report_bytes`: `Row,Column,Problem`, every error collected, CSV-injection-guarded; surfaced on New Challan + Batches). Gate: BE ruff 0 · mypy 39 · pytest 172 · FE tsc + build + vitest 24. Commit `0e252d7`.

### Increment 10 — Register reports + Numbering view + auth scaffolding + sweep scheduler (orchestrated: 4 build agents + 1 audit → I integrated/gated/runtime-verified/fixed)
- **Register reports:** `date_from`/`date_to` (inclusive) filter on `GET /challan/challans`; `GET /challan/challans.csv` export — shared `_register_query`, `service._csv_field` injection guard (RFC-4180 quote-**doubling** for fidelity), money as plain 2dp decimals, 50k cap that is **never silent** (trailing marker row + `X-Truncated` header). FE: From/To date inputs + Download CSV button (`downloadUrl`, parses friendly detail errors).
- **Numbering register view:** new module tab — counters (per-series high-water mark) + allocations register (status filter, load-more, bound-to entity, void reason), consuming the module-gated `/numbering/counters` + `/numbering/allocations`.
- **Auth role-guard scaffolding:** `RequireRole` (fails CLOSED) + `useHasRole` + a LOCAL dev role-switcher kept OUT of `AuthContextValue` (mock-only `MockDevContext`; the real FirebaseAuthProvider contract is intact). **Prod builds FAIL CLOSED** — the mock auto-admin provider is disabled in a production build (renders a hard "auth not configured" block). Route access stays governed by per-user module-access, NOT a role allowlist.
- **Sweep scheduler IaC:** `infra/terraform/scheduler.tf` — hourly (Asia/Kolkata) Cloud Scheduler → `POST /numbering/sweep` with `X-Sweep-Secret`; secret wired via secrets.tf/cloudrun.tf/iam.tf; added the **missing `allUsers` run.invoker** binding the public SPA + scheduler both need. `terraform validate` Success. NOT applied (owner GCP).
- **Independent audit (1 HIGH/2 MED/5 LOW) — money/authz/auth-contract core CLEAN; fixed:** H1 prod-auth fail-closed · M1 invoker binding · M2 CSV silent-truncation signal · L2 quote fidelity · L4 friendly download errors · L5 copy. **Not changed (surfaced, not silently deferred):** L1 offset-paging boundary duplicate (inherent to offset paging; low) · L3 CSV UTF-8 BOM (parser tradeoffs; data is mostly ASCII). **Caught + corrected during integration:** a build agent gated the challan route with a role allowlist `['ADMIN','OPERATIONS']` — that contradicts the per-user module-access model (would block a granted MIS user, admit an un-granted OPERATIONS user); removed.
- **Gate:** BE ruff 0 · mypy 38 · pytest 168 · alembic no-drift · FE tsc + vite build + vitest 28 · terraform validate Success. Commit `e66a0c8` (code+tests); docs in the follow-up commit.

### Increment 9 — Overview metrics + register pagination + 2nd audit pass (orchestrated: 3 agents → I integrated/gated/runtime-verified/fixed)
- **Overview tab** (module landing at `/m/document_automation`): `GET /challan/summary` aggregation — issued/void/eway/valued counts + total value + by-series breakdown; **VOID excluded from value**, a value-free ISSUED challan counted but not valued. FE stat tiles + breakdown table + FY/series filters. *(runtime-verified: a VOID challan's ₹ value is excluded from Total value live.)*
- **Register load-more**: offset paging replaces the silent 100-row cap — "Showing N" footer, load-more only while a full page returns (`useChallansInfiniteQuery`). *(closes a silent-truncation gap.)*
- **Independent adversarial audit (money/void/numbering) — NO HIGH; core confirmed hardened** (SAVEPOINT trap closed, GSTIN mod-36 checksum, CSV-injection guard, e-way off-by-one, audit hash-chain all CLEAN). **6 MED/LOW findings fixed + regression-tested** (`tests/test_audit_fixes.py`): **MED-1** generate-wedge — any parse/reserve-phase error now marks the batch FAILED, not stuck in committed GENERATING · **MED-2** `eway_threshold` ≤0 → statutory default (never silently disables e-way) · **MED-3** Excel-serial date overflow → clean row error, not a 500 · **MED-4** numbering-void route refuses an ISSUED number bound to a live challan (would orphan it + permanently 409 its proper void) · **LOW-1** atomic single-worker generate claim (conditional UPDATE) · **LOW-3** upload cap rejects before the buffer can exceed the limit.
- **Deliberately NOT changed (surfaced, not silently deferred):** LOW-2 series-scoping the idempotency key would reintroduce a partial-retry duplicate risk — current fail-closed behavior kept; LOW-4 the single float→str→Decimal touch is safe under Python's round-tripping repr.
- **Reprint was found ALREADY built** (Register per-challan PDF + Batches ZIP/merged/error re-download) — not rebuilt (reconciled against reality before building).
- **Gate: BE ruff 0 · mypy --strict 38 · pytest 161 · alembic no-drift · FE tsc + vite build + vitest 14.** Commit `6c38d2b` (code+tests); docs in the follow-up commit.

### Increment 2 — platform primitives + frontend shell (orchestrated: 4 agents → I integrated/gated/audited)
- **Backend primitives (gate-green: ruff 0 · mypy --strict 0 · pytest 23/23):**
  - `app/modules/files/` — upload + **streamed authed download** (storage abstraction `platform/storage.py`, LocalStorage path-safe, GcsStorage TODO). Table `files_stored_file` (+ `module_key`).
  - `app/platform/jobs.py` + `app/modules/jobs/` — idempotent async jobs (`job` table), polling `GET /api/v1/jobs/{id}`.
  - `app/modules/settings/` — admin-gated settings CRUD (audited).
  - Wired into `main.py`: files at `/api/v1/files`; jobs + settings at `/api/v1`.
  - **Alembic** baselined: `alembic.ini` + `env.py` (imports all model modules) + migration `e215e02932b5_baseline_platform_tables` — applies cleanly to sqlite (7 tables).
- **Frontend shell** (`frontend/`, gate: `tsc` + `vitest` + `build` all green): Vite+React+TS+Tailwind+React-Query; app shell, nav from `/api/v1/modules`, dashboard tiles, mock auth behind a swappable `AuthProvider` (FirebaseAuthProvider TODO). Build output → `frontend/dist`.
- **Independent adversarial audit run + ALL findings fixed** (this is why the audit is mandatory):
  - CRITICAL **download IDOR** → download authz gate (uploader OR admin OR `can_access_module(module_key)`), 404 (not 403) on miss. *(proven: `test_download_denied_for_other_user`)*
  - HIGH **jobs IDOR** → `get_job` scoped to creator/admin, 404 on miss.
  - HIGH **idempotency ran work twice** → `create_job` returns `(job, created)`; worker scheduled only when `created`.
  - HIGH **upload DoS** → `max_upload_bytes` cap → 413. *(proven: `test_upload_rejects_oversized_file`)*
  - MED **job transitions** → state-machine guards (no double-complete). *(proven: `test_terminal_job_cannot_be_recompleted`)*
  - MED **idempotency rollback nuked session** → SAVEPOINT (`begin_nested`).
  - MED **audit-chain concurrent fork** → `prev_hash` UNIQUE + retry-against-head.
  - MED **RBAC fail-open** → `can()` now DEFAULT-DENY (unknown action → False). *(proven in `test_smoke`)*
  - LOW **PII in audit / header injection** → filenames out of audit `detail`; RFC5987 disposition + control-char strip.

### Decisions / flags (from increment 2)
- **Settings hold CONFIG only, never secrets** (secrets → Secret Manager). Reads are open to authenticated users (modules need config); writes are Admin-only + audited. Documented policy, not a code gate.
- ⚠️ **Migration was autogenerated on sqlite — VERIFY it on Postgres before staging** (Enum types render differently; hand-check the `user.role` / `job.status` enum DDL on a real Postgres).

### Increment 1 — repo + spine

### Done + verified (this increment)
- Repo initialised; structure: `backend/` (FastAPI) · `frontend/` · `infra/terraform/` · `.github/workflows/`.
- **Backend spine, gate-green** (`ruff` 0 · `mypy --strict` 0 · `pytest` 4/4):
  - `app/config.py` — pydantic-settings, env-driven.
  - `app/db.py` — SQLAlchemy engine/session (sqlite local bootstrap; Postgres in staging/prod).
  - `app/platform/module_registry.py` — the `register_module(ModuleSpec)` convention (no plugin loader).
  - `app/platform/models.py` — platform tables: `user`, `user_module_access` (**explicit per-user grants**), `audit_log` (hash-chained), `setting`.
  - `app/platform/rbac.py` — `can(user, action, resource?)` + `can_access_module(user, module)`; ADMIN_ONLY action set.
  - `app/platform/audit.py` — append-only **hash-chained** audit (`log`, `verify_chain`); tamper-evident (test-proven).
  - `app/platform/auth.py` — Firebase token verification dependency (`current_user`); valid-token-but-no-active-row → denied.
  - `app/modules/health/routes.py` — public `/api/v1/health` + `/api/v1/modules` registry echo.
  - `app/main.py` — composes registered modules under `/api/v1/<key>`.
  - `tests/test_smoke.py` — health, registry, audit-chain tamper-evidence, RBAC two-axes.
- Ops: `requirements.txt`, `pyproject.toml` (ruff/mypy/pytest), `Dockerfile` (python:3.12-slim), `.github/workflows/ci.yml` (the `test` gate), `.gitignore`, `.env.example`.

### Gotchas already hit (don't re-hit)
- **Python 3.14 + SQLAlchemy:** 2.0.36 crashes on `str | None` Mapped columns (`make_union_type` / changed `typing.Union`). **Pinned `sqlalchemy==2.0.51`.** Prod container uses **python:3.12-slim** to avoid the edge entirely.
- Models module must NOT use `from __future__ import annotations` (SQLAlchemy de-stringify path).
- FastAPI deps use the **`Annotated[...]`** style (avoids ruff B008).

### How to run the gate
```
cd backend && python -m venv .venv && ./.venv/Scripts/python.exe -m pip install -r requirements.txt
./.venv/Scripts/python.exe -m ruff check app tests   # 0
./.venv/Scripts/python.exe -m mypy app               # 0
./.venv/Scripts/python.exe -m pytest                 # all pass
```

### Increment 3 — seed + Terraform (done + gate-green)
- **`app/seed.py`** — guarded (fail-closed for prod), idempotent; seeds an Admin + an MIS user (explicit `document_automation` grant) + `eway_threshold` setting. *(pytest 26/26 incl. prod-refusal + idempotency.)*
- **`infra/terraform/`** — full separate-project infra, **`terraform validate` Success** (fmt clean): project + APIs · VPC + Private Service Access · **Cloud SQL Option-B** (one small single-zone instance, prod+non-prod DBs, backups/PITR, private IP) · GCS (isolated) · least-priv SAs · Secret Manager (`@localhost/…?host=/cloudsql/…` form) · **Cloud Run scale-to-zero** + **in-VPC Alembic migrate job** · billing budget + threshold alerts. **APPLY needs owner GCP org/billing access.**

### Increment 4 — same-origin SPA serving + CI/CD (done + gate-green)
- **FastAPI serves the built SPA same-origin** (`config.static_dir` + `_mount_spa` in `main.py`): real files served, client routes fall back to `index.html`, `/api/*` still resolves (and 404s as API). *(pytest now 31/31; 5 SPA tests.)* Local dev leaves `static_dir` empty (Vite dev server + proxy).
- **Root multi-stage `Dockerfile`** — builds the SPA then bakes it into the FastAPI image (`STATIC_DIR=/app/static`). One image = whole product; used by the Cloud Run service AND the migrate job. (`backend/Dockerfile` removed.)
- **CI/CD** (all 4 workflows YAML-valid): `_test.yml` (reusable full gate — backend ruff/mypy/pytest + frontend tsc/vitest/build) · `ci.yml` (PRs/pushes) · `deploy-staging.yml` (auto on `develop`, `deploy` **needs `test`**, build→push→**in-VPC migrate `--wait`**→deploy→**verify serving SHA**) · `deploy-prod.yml` (on `main`, `environment: production` **required-reviewer gate**).

### Increment 5 — Delivery Challan: NUMBERING ENGINE (done + gate-green + triple-audited)
The load-bearing, statutory numbering core. New module `app/modules/numbering/` (models · service · routes) + migration `ad8a9c93c1ac` + `tests/test_numbering.py` (34) + `tests/test_numbering_routes.py` (7). **Gate: ruff 0 · mypy --strict 27 files · pytest 72 · `alembic check` no drift · fresh migrate clean.**
- **Number format LIVE:** `GIF/DC/{FY}/{series}/{NNNNNN}` e.g. `GIF/DC/26-27/L/000189` — FY (Apr–Mar) computed in **Asia/Kolkata** (`tzdata` pinned), series upper-alnum, 6-wide, **resets each FY**. Modes 1–2 (continue-from-last, custom-start via `seed_series`); read-from-Excel deferred.
- **Integrity (§B3) built + PROVEN at runtime:** reserve→issue→void lifecycle · unique **partial index on non-void `(series, fy, number)`** (modelled in `__table_args__`, hand-applied in migration, `status` CHECK) · idempotency-resume (scoped to series/fy) · progress-making retry against the backstop · counter high-water monotonic/no-reuse · reconcile sweeper (atomic guarded UPDATE) · every mutation audited in-txn · **allocate refuses an unconfigured series** (no accidental `000001`).
- **DUAL adversarial audit + FIX re-audit (money-path mandate):** caught + fixed real defects a green gate hid — platform `verify_chain` false-positive (sqlite tz round-trip, **fixed in `audit.py`**), sweeper lost-update clobbering an ISSUED challan, issue-after-void TOCTOU, unaudited issuance, cross-sequence idempotency key returning the wrong number, unconfigured-counter footgun, seed/fy/width bounds. All reproduced + fixed + regression-tested. See [[audit-every-build-item]].
- ⚠️ **Not yet wired to a consumer:** `service.allocate`/`issue`/`sweep` have no live caller until the challan generator (below). The transaction contract (reserve→commit-promptly→generate→issue) is documented in the service; enforce it in the consumer.

### Increment 6 — Master data + Challan GENERATOR (done + gate-green + dual-audited)
The numbering engine's first real consumer + its reference data. New modules `app/modules/masterdata/` (models · routes · normalize) + `app/modules/challan/` (schema · models · parsing · render · service · routes) + migrations `e313362febfe` (master data) + `c57f9ac224ca` (challan) + deps `openpyxl`/`pypdf`/`weasyprint`. **Gate: ruff 0 · mypy --strict 38 files · pytest 125 · `alembic check` no drift.** Orchestrated: parsing + render built by parallel sub-agents against frozen `schema.py` contracts; I built models/service/routes and integrated.
- **Master data:** Consignor · **Brand→State→{GSTIN,address} consignee registry** · HSN(+GST rate) · series. Admin CRUD (`masterdata.edit`) + module-gated reads, all audited. Consignee **case-insensitive-unique** (normalized on write + `lower(trim())` unique index) so resolution/snapshot is never ambiguous.
- **Generator flow:** upload (chunked-capped, uuid key) → **validate** (structural + master-data + amount-sanity + HSN-rate; downloadable English error report, CSV-injection-guarded) → **reserve ALL numbers + commit BEFORE render** (closes the numbering lock-window) → HTML→PDF (WeasyPrint, escaped/bind-as-data, `data:`-only url_fetcher) → **issue** → ZIP + merged PDF → register. Consignor/consignee re-resolved + **snapshotted** onto each challan. e-way flag from `eway_threshold` setting. Admin void (challan + its number). Per-batch caps. **Crash-safe + resume-safe**: a render failure marks the batch FAILED + voids orphaned reservations; retry resumes without double-issuing.
- **DUAL adversarial audit (2 lanes: correctness/money + security) — both found real bugs a green gate hid, ALL fixed + regression-tested:** ⚠️ **a SAVEPOINT leak in `numbering.allocate` + `audit.log` + `jobs.create_job`** (bare `begin_nested()` never released → `RecursionError` at ~250 ops in one txn) — **fixed platform-wide** with `with db.begin_nested():`, proven by a 500-in-one-txn test; plus partial-generation wedge, unsafe idempotent resume (dup `allocation_id`), unbounded-batch statutory-number burn, WeasyPrint SSRF (url_fetcher), whole-file-read DoS, filename→500, CSV formula injection, float money display, e-way parse crash. Master-data audit's HIGH (consignee case/space duplication → wrong GSTIN) fixed too.
- ✅ **Increment 7 — FAITHFUL L/433 LAYOUT (`fdbdb83`, gate-green pytest 129):** reworked to the real doc — single fixed consignor, 5-field ship-to (name/address/enterprise/number/contact), line table `Sl.No|Product|HSN|Qty|Rate|Amt (incl Tax)` (UOM/GST% dropped from print), (Not for Sale) + Total(qty,amt) + Authorized Signatory, ordinal date, **Indian-grouped rupees**. **Amount is OPTIONAL (value-free challan) and TAX-INCLUSIVE** when present: validated `Rate×Qty×(1+GST)` + supplied-GST must equal HSN GST (owner-confirmed). Phones added to master data; consignee resolved case-insensitively. Render template rewritten (escaping/url_fetcher intact). **Focused money re-audit DONE (`f856d46`) — fixed 2 HIGH (value-free challan 500'd the list API; mixed priced/value-free lines under-reported total→wrong e-way) + amount-tolerance abs-cap + e-way strict-`>` + negative-rate reject; all regression-tested.**
- ⚠️ **SURFACED (owner calls):** (a) GSTIN full **checksum + state-name↔code cross-check DONE** (`ea23539`); (b) **intra-module IDOR is by-design** (single-tenant): any `document_automation` user can read all batches/challans + download others' uploaded **source spreadsheets** (may hold pricing) — scope to uploader/admin if that's sensitive; (c) layout now matches `L/433` (inc 7) using the real doc; **real WeasyPrint PDF render still to verify in the Linux container** (not runnable on Windows) + owner eyeball of the browser-preview sample.

### Increment 8 — FRONTEND (Delivery Challan UI) + supporting backend (done + gate-green + UI/UX-audited + runtime-verified)
The whole challan flow is now clickable. **FE gate: tsc --noEmit · vite build · vitest 8; Backend gate: ruff 0 · mypy 38 · pytest 143 · no drift.** Orchestrated: I owned the shared foundation (UI kit `src/ui/*` · api-client put/del/download/postForm · `ChallanModule` shell + routing · backend nav registration); 3 parallel agents built master-data screens, challan screens, and the sweeper endpoint; I integrated + ran a mandatory UI/UX audit + fixed.
- **Screens** (`frontend/src/pages/challan/*`): **New Challan** (upload→validate→error-report / counts→generate→poll→ZIP+merged download + blank-template), **Batches** (status + artifacts), **Register** (filters · e-way badge · "—" for value-free · **admin Void** via confirm+reason), **Master Data** (Consignor/Consignee/HSN/Series CRUD, soft-disable, field-level 422 errors). Invoice number now prints on the challan **just above the Delivery Challan No.** (owner ask).
- **Supporting backend:** invoice line in render; `document_automation` nav ModuleSpec; **secret-gated `POST /numbering/sweep`** (`X-Sweep-Secret`, 503 if unconfigured) for the reconcile scheduler; **LOCAL-ONLY dev-auth shim** (`DEV_AUTH=1` + `env=local`, double-guarded, inert in staging/prod) so the mock-admin SPA drives the real backend before Firebase. Fixed a **pre-existing tsconfig bug** (FE typecheck gate was never actually green).
- **UI/UX adversarial audit — found real defects a green gate hid, ALL fixed + verified live:** 422 array `detail`→"[object Object]" (root-caused in `client.ts` + unit-tested), Modal had no focus trap/restore, Modal dismissable mid-submit (now `busy`-guarded), infinite generate-poll with no escape (Start-over + stall hint), query-client recreated per render, TZ-off-by-one date, unassociated file label.
- **Runtime-verified in the browser** (backend+Vite, seeded demo DB): register renders real challans w/ correct ₹ + e-way; the **void destructive path proven end-to-end** (UI→API→DB: challan + its number VOID with reason); master-data + upload screens render; no console errors.

## ▶ IMMEDIATE NEXT
- **✅ Through inc 27 all queued CODE is DONE** — Phase-1 challan + Phase-2 v1 Expense/Invoice + inc-26 RBAC v2 + inc-27 expense cost-allocation (Project + Payment method + spend dashboard) are feature-complete + audit-hardened + E2E-verified. **No code work is queued;** only the owner-gated infra + the small decisions below remain.
- **✅ Inc-27 follow-ups DONE:** the catch-all **"General / Overhead" project** is now seeded (GEN-001, idempotent; reusable `seed.ensure_overhead_project` for the future prod bootstrap), and the Overview dashboard + register now show the project **name** alongside its code.
- **RBAC v2 is ready for real auth:** the `GET /me` effective-permissions contract + `usePermissions()`/`RequirePlatform` are wired so the future `FirebaseAuthProvider` (AuthProvider.tsx TODO) drops in and gets permissions for free. **PROD BOOTSTRAP prereq (deploy pass):** call `roles_builtin.ensure_builtin_roles` + create the first Administrator user (the migration creates the Administrator ROLE but no one holds it; `app.seed` is fail-closed on prod). **Verify the RBAC migration on Postgres** (the enum table/type collision + type-orphan fixes are dialect-guarded but SQLite-invisible).
- **Owner-gated Phase-1 remainder:** real **WeasyPrint PDF render in the Linux container** + byte-match `L/433` (can't run on Windows) · **real Firebase auth** end-to-end (replace mock `AuthProvider` + dev-auth shim → per-role gating) · **deploy to staging** + verify the migration on Postgres + E2E · **apply** the already-written Cloud Scheduler sweep IaC (`infra/terraform/scheduler.tf`).
- **Owner inputs to go live:** real `L`-seed number · real master data (consignor; consignees now self-populate via the GSTIN master; **Projects must be created before challans reference them**) · GST retention · source-file IDOR/scoping call.
- **Already DONE (no longer pending here):** Overview dashboard · register reports (date + CSV) · Numbering view · challan reprint · self-documenting template · sweep scheduler IaC (written, not applied) · auth role-guard scaffolding · Projects module · consignee GSTIN master · 26-col template + inline consignee + Project ID + validation warnings (inc 15) · consignee contradiction review (accept/reject per field) + Excel report (inc 16) · **resume-aware generate / retry-after-partial reconcile (inc 17)**.

## Later (Phase 0 remaining)
Access-independent (buildable now): **Settings-backed config surface** (thin, e.g. e-way threshold).

Blocked on owner GCP/Firebase access (needed for runtime-verification):
5. **User Management** module (Admin) — create/invite via Firebase Admin SDK, assign role + explicit module access, enable/disable; audited. *(auth path → dual audit; needs Firebase.)*
6. **Real Firebase auth** end-to-end — `FirebaseAuthProvider` in the FE + seeded admin → real token → `current_user` → role/module gating runtime-verified per role.
7. **E2E harness** (Playwright, real Firebase login per role, prod build) + first green run.
8. **Verify the migration on Postgres** (enum DDL) before staging.

## Needed from owner
- **No git remote is configured yet** (`git remote -v` is empty; `develop`/`main` are local-only). To push/CI/deploy, add the GitHub `origin` first — then the "push `develop` → staging auto-deploy" flow can run (still needs the deploy secrets below).
- **GCP + Firebase access** to provision/deploy (Terraform written + validate-clean, not applied without it).
- **Deploy wiring after GCP** (for the CI/CD workflows): GitHub secrets `WIF_PROVIDER`, `DEPLOY_SA`; vars `GCP_PROJECT_ID` (staging) + `GCP_PROJECT_ID_PROD`; an Artifact Registry repo `opshub`; and a `production` GitHub Environment with required reviewers (the prod gate).
- The 4 module inputs (Phase 1/2): second use case · challan `L`-seed · master data · GST retention.

## Conventions
- Branch `develop` (staging), `main` (prod, gated). Keep `develop` unpushed until locally gate-green. No push/deploy/prod action without owner go (WoW #8).
