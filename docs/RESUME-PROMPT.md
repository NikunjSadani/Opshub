# Compact resume prompt — Gifsy OpsHub

Paste the block below into a fresh session to continue the build cold.

---

Continue building **Gifsy OpsHub** (internal ops platform). Repo: `C:\Users\nikun\gifsy-opshub`.

**First, read (in order):** `docs/RESUME.md` (live build state — authoritative) → `docs/DESIGN.md` (v4 architecture) → `docs/BUILD-PLAN.md` (governance + phases). Also load the `opshub-platform` memory and the WAYS-OF-WORKING standing agreements.

**State — `develop` @ `4669341`, UNPUSHED, gate-green** (BE: ruff 0 · mypy --strict 45 files · pytest ~240 · `alembic check` no drift · FE: `tsc --noEmit` · `vite build` · vitest 37 · `terraform validate` clean). Stack: Python/FastAPI + React SPA (same-origin) · Firebase Auth (email/pw, not yet wired) · Postgres (sqlite local) · separate GCP project · DB Option-B · Cloud Run scale-to-zero · ~₹2k/mo.

**Phase 1 (Delivery Challan) is CODE-COMPLETE end-to-end; inc 5–16 landed** (each dual/UI-UX-audited + runtime-verified). The 3-part consignee/project thread (inc 13→14→15) is COMPLETE, and inc 16 adds the consignee contradiction review:
- **Inc 16 (`4669341`) — consignee contradiction review (accept/reject per field) + Excel report:** an upload that contradicts the stored consignee golden record (known GSTIN) sends the batch to a new **NEEDS_REVIEW** state (BLOCKED from generation) with one **decision per differing field** — `UPDATE_MASTER` (permanently updates the shared golden record + prints uploaded), `THIS_UPLOAD` (prints uploaded on this batch only, master untouched), `REJECT` (keeps + prints stored). Batch → VALIDATED only when none PENDING; a PENDING field never prints an unvetted upload (defaults to stored); the golden-record write is deferred to batch SUCCESS. SAME GSTIN with inconsistent details in one upload = blocking error. Downloadable **Excel** review report (uploaded rows + contradictions sheet). New `challan_batch_decision` table + `DecisionChoice` enum, migration `8a1ab1024872`; row cap → 50000 (2000-challan cap is the only real limit). FE `ReviewPanel` reachable from the upload flow AND the Batches tab; "Update master" has honest permanent/shared-consequence copy + confirm. **DUAL + UI/UX audit — no HIGH in core gating; fixed+regression-tested:** Excel formula/DDE-injection guard · un-resumable-batch (now Batches-tab resolvable) · UPDATE_MASTER not applied before generation success · control-char cell no longer 500s upload · new-GSTIN merged superset · UPDATE_MASTER admin-gated · dup decision-id 422 · submit row-locked. ▶ **1 owner decision still parked (non-blocking):** full auto-reconcile of a retry-after-partial-generation (minimal guard in). The inc-15 "sparse golden record" decision is RESOLVED by inc 16 (contradictions are now reviewed, not auto-filled).
- **Numbering engine** (`app/modules/numbering`): reserve→issue→void, FY in Asia/Kolkata, `GIF/DC/26-27/L/000189`, unique partial index on non-void `(series,fy,number)` + status CHECK, idempotency-resume scoped to series/fy, configured-series guard, in-txn audit, reconcile sweeper + secret-gated `POST /numbering/sweep`.
- **Master data** (`app/modules/masterdata`): single Consignor · Brand→State→{GSTIN,address} consignee registry (case-insensitive-unique) · HSN(+rate) · series; admin CRUD; full GSTIN checksum + state cross-check. **+ a NEW GSTIN-keyed `ConsigneeParty` golden-record master (inc 14) that supersedes the brand→state registry in inc 15.**
- **Challan generator** (`app/modules/challan`): upload→validate (structural+master-data+**tax-inclusive** amount `Rate×Qty×(1+GST)`, supplied-GST==HSN, CSV-guarded English error report)→**reserve ALL + COMMIT before render (lock-window CLOSED, proven)**→WeasyPrint HTML→PDF (**faithful L/433 layout**, escaped, `data:`-only url_fetcher)→issue→ZIP+merged→register; snapshots; amount OPTIONAL (value-free); invoice-no prints above challan-no; admin void; per-batch caps; crash/resume-safe.
- **React frontend** (`frontend/src`): UI kit `ui/*` + `pages/challan/*` — **Overview** (metrics: issued/void/eway counts + total value + by-series, from `GET /challan/summary`), New Challan (upload→generate→poll→download), Batches, Register (filters/e-way/admin Void + **load-more pagination**, no silent 100-row cap), Master-Data CRUD. Reprint already exists (Register per-challan PDF + Batches ZIP/merged). Built on `useApi()` (get/post/put/del/download/postForm) + react-query. **Auth is a MOCK admin** + a LOCAL-ONLY dev-auth shim (`DEV_AUTH=1`+`env=local`, double-guarded) so the SPA drives the real backend pre-Firebase.
- **2nd adversarial-audit pass (inc 9) done** — money/void/numbering core confirmed hardened (no HIGH); 6 MED/LOW fixed + regression-tested (`tests/test_audit_fixes.py`): generate never wedges in GENERATING · `eway_threshold`≤0→statutory default · Excel-serial date overflow→row error · numbering-void refuses an ISSUED number bound to a live challan · atomic single-worker generate claim · upload-cap tightened.
- **Inc 10 (`e66a0c8`) — register reports + numbering view + auth scaffolding + sweep IaC (all solo, audited):** `GET /challan/challans.csv` + `date_from`/`date_to` on the register (injection-guarded, RFC-4180 quote-faithful, 50k cap with `X-Truncated` signal — never silent); a Numbering register tab (counters + allocations); `RequireRole` guard + local dev role-switcher (mock-only, `AuthContextValue` unchanged) with **prod builds FAIL CLOSED** (mock auto-admin disabled in a prod build); and `infra/terraform/scheduler.tf` wiring hourly Cloud Scheduler → secret-gated `/numbering/sweep` (+ the missing `allUsers` run.invoker binding). Audit fixed 1 HIGH/2 MED/3 LOW; module access is by per-user grant NOT role (a wrong role-allowlist route guard was caught + removed). Reports tests in `tests/test_challan_reports.py`.
- **Inc 11 (`0e252d7`) — self-documenting upload template:** `GET /challan/template.xlsx` (`challan/template.py`, module-gated) replaces the header-only CSV — a **Challans** data sheet (header hover-comments + 2 worked example rows) + an **Instructions** sheet (per-column Required?/what-to-enter DERIVED from the schema + Dos & Don'ts). Round-trip test proves the examples parse+validate. The downloadable **English validation error report** (`Row,Column,Problem`, all errors, injection-guarded) already existed on the upload/Batches path.
- **Inc 12 (`291855e`) — New Challan 3-step redesign + UI/UX audit:** numbered 1-2-3 step cards (Get template → Upload & validate → Number & generate, Step 3 disabled-with-reason until validated; error report first-class) + a Recent-batches list. Borrowed ONLY the layout/steps/recent-batches from an external "Challan Desk" concept — kept our server-reserved numbering (not a typed starting number). Audit fixed a HIGH app-wide `Button` double-submit (`disabled ?? loading` → `(disabled ?? false) || loading` in `ui/primitives.tsx`), + MED (batches-list invalidate on terminal, sr-only aria-live) + LOWs.
- **Inc 13 (`76065f8`) — Projects module (NEW dashboard module):** `app/modules/projects/` + `pages/projects/`. Admin registers a **Client** (unique 3-letter code); users create **Projects** → system-assigned `<CLIENT_CODE>-<per-client running no.>` (`BRI-001`, `TAT-001`) via a per-client row-locked sequence (reuses numbering DISCIPLINE, not the challan counter; UNIQUE(code)+UNIQUE(client_id,seq) backstops). Migration `690e7736007c`, `ModuleSpec(key="projects")`, `project.manage` admin. Audit: integrity core CLEAN; fixed contention→409, no raw-error leak, READ-COMMITTED documented, name-cap.
- **Inc 14 (`7094f15`) — consignee golden-record master (GSTIN-keyed):** NEW `ConsigneeParty` (`app/modules/masterdata/`, migration `d61ab46c13de`), additive — legacy brand→state `Consignee` + challan parser UNTOUCHED. `consignee_master.resolve_or_create`: unknown GSTIN **auto-creates (source UPLOAD, NO verify)**; known GSTIN w/ diffs → **stored record + deviation report, never overwrites**. Admin CRUD + new "Consignees (GSTIN)" Master Data tab. Identity audit: core CLEAN; fixed **gstin↔state enforced on CREATE** (422) + no-op-PATCH guard.
- **Inc 15 (`28b627d`) — 26-col template · inline GSTIN consignee · Project ID · warnings (DONE):** friendly-headed 26-col template (alias map) with explicit `Challan Group`, structured ship-to (split-store/joined-print), inline consignee resolved by GSTIN, `Project ID`; **brand DROPPED**. Consignee auto-creates AT GENERATION via `consignee_master.resolve_or_create` (new `dry_run` at validation → a failing batch leaves no golden records); known-GSTIN diffs = non-blocking DEVIATION WARNING, STORED record snapshotted; **Project ID must pre-exist + be Active** (blocking) + printed; possible-split + intra-batch-new-GSTIN warnings. Migration `3dab8e347c22` (project_code + structured ship-to cols; `consignee_brand`→nullable). DUAL + UI/UX audit fixed: HIGH `parse_paise` 500 on `1E30` + money ceiling · HIGH golden-record UPDATE now enforces GSTIN↔state (422) · MED intra-batch conflict warns · MED retry-after-partial preserves issued challans · LOW collision date-aware + pincode ASCII · UI/UX HIGH warnings-label status-derived — all regression-tested. **▶ 2 owner decisions open (non-blocking):** (a) sparse golden record → blank statutory field: fill-in-empty [recommended] vs block vs status-quo-warning; (b) full auto-reconcile of a retry-after-partial-generation (minimal guard already in).

**Known platform trap (fixed, don't reintroduce):** a bare `db.begin_nested()` that never releases leaks a SAVEPOINT → `RecursionError` at ~250 ops/txn. Always use `with db.begin_nested():`. (Was in numbering/audit/jobs.)

**How to work (WAYS-OF-WORKING + BUILD-PLAN Part 1):** orchestrate build streams to sub-agents; I own the shared foundation + run the FULL gate (never trust a piped exit code) + runtime-verify + an INDEPENDENT adversarial audit per build item (DUAL on money/auth/void; UI/UX lane for user-facing). Runtime = definition of done. Keep `develop` unpushed until locally green; commit gate-green increments. **No push / no cloud / no irreversible action without owner go.**

**Phase 1 remaining (owner-gated — inc 15 is DONE; the immediate CODE next is whichever of the 2 open decisions above the owner picks):** real **WeasyPrint PDF render in the Linux container** + byte-match `L/433` (can't run on Windows); **real Firebase auth** end-to-end (replace mock AuthProvider + dev-auth shim → per-role gating); **deploy to staging** + Postgres migration verify + E2E; **apply** the already-written Cloud Scheduler sweep IaC. *(Reprint · Overview dashboard · register reports · Numbering view · sweep IaC · Projects · consignee GSTIN master · 26-col template/inline-consignee/Project-ID/warnings — already DONE, inc 9–15.)*

**Blocked on owner** (GCP project + Firebase + deploy secrets `WIF_PROVIDER`/`DEPLOY_SA`/`GCP_PROJECT_ID` + `production` GitHub Environment): real auth per role, User-Management module, deploy, container PDF render, E2E, Postgres migration verify. **Owner inputs/decisions:** real `L`-seed number · real master data (consignor + brand→state-GSTIN registry) · GST retention · **source-file download scoping** (intra-module IDOR is by-design single-tenant — scope to uploader/admin if source spreadsheets are sensitive) · the Expense-module (Phase 2) second use case.

**Run locally:** backend `DATABASE_URL=sqlite:///./_demo.db ENV=local DEV_AUTH=true uvicorn app.main:app --port 8000` (migrate + `python -m app.seed` first); FE `npm --prefix frontend run dev` (proxies /api→8000). Gates: BE `./.venv/Scripts/python.exe -m {ruff check app tests, mypy app, pytest}`; FE `npm --prefix frontend run {typecheck,build,test}`.

---
