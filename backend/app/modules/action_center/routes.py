"""Action Center API — filled by the Wave-4 agent. RBAC key ``action_center`` (VIEW)."""
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()

MODULE_KEY = "action_center"
