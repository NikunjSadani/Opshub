# Runtime
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
sqlalchemy==2.0.51              # >=2.0.51 for Python 3.14 typing.Union compat
alembic==1.14.0
psycopg[binary]==3.2.3          # Postgres (staging/prod)
firebase-admin==6.6.0            # auth: token verification + user provisioning
python-multipart==0.0.20        # file uploads
tzdata==2024.2                   # zoneinfo Asia/Kolkata FY calc (Windows + slim containers have no system tzdb)
openpyxl==3.1.5                  # challan Excel upload parsing
pypdf==5.1.0                     # merged-PDF assembly
pdfplumber==0.11.10              # expense: text-layer invoice extraction (words + tables + boxes; pure-python, zero-cost)
weasyprint==63.1                 # HTML->PDF challan render (lazy-imported; needs GTK native libs, Linux container only)
segno==1.6.6                     # pure-python QR (no native deps); inline-SVG QR stamped on the challan PDF

# Dev / gates
ruff==0.8.4
mypy==1.14.0
pytest==8.3.4
httpx==0.28.1                    # TestClient
reportlab==4.2.5                 # TEST-ONLY: generate synthetic text-layer invoice PDFs for the extractor + eval fixtures (not imported by runtime code)
google-cloud-storage==2.19.0
